# Ioband

Getting started

1. Create a copy of `src/secrets example.h` named `secrets.h` and feel with the
   data for WiFi connection. Make sure to not include it in the commits.
2. Compile and upload the project to the device.
